pipeline {

  agent any

  environment {
    registry = "vaporio/deployment_tools"
  }

  stages {
	stage('Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Build') {
      steps {
        script {
          docker.build registry + ":$BUILD_NUMBER"
        }
      }
    }
    stage('Publish') {
      when {
        branch 'master'
      }
      steps {
        withDockerRegistry(registry: [credentialsId: 'vio-docker-hub']) {
          sh 'docker push ${registry}'
        }
      }
    }
  }
}
